services:
  - type: web
    name: mcp-on-render
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /healthz
    runtime: docker
    region: oregon  # pick closest to your users
    envVars:
      - key: AUTH_TOKEN
        generateValue: true  # Render will generate a secret; or set your own value
